# 🎨 نظام النسخ المتعددة (Version System)

## ✨ نظرة عامة

تم إضافة نظام versioning يسمح لك بالتبديل بين **3 نسخ مختلفة** من التصميم بسهولة أثناء عرض المشروع للعميل:

- **Hybrid** - النسخة الهجينة (المجلد v1)
- **Invite** - نسخة الدعوات (المجلد v2)
- **Public** - النسخة العامة (المجلد v3)

---

## 📁 هيكل المشروع الجديد

```
jisor-mass-campaign/
├── css/
│   ├── v1/               # Hybrid - النسخة الهجينة
│   │   ├── styles.css
│   │   ├── applications.css
│   │   ├── accepted-pool.css
│   │   └── proposal-details.css
│   ├── v2/               # Invite - نسخة الدعوات
│   │   ├── styles.css
│   │   ├── applications.css
│   │   ├── accepted-pool.css
│   │   └── proposal-details.css
│   └── v3/               # Public - النسخة العامة
│       ├── styles.css
│       ├── applications.css
│       ├── accepted-pool.css
│       └── proposal-details.css
├── js/
│   ├── v1/               # Hybrid scripts
│   │   ├── app.js
│   │   └── accepted-pool.js
│   ├── v2/               # Invite scripts
│   │   ├── app.js
│   │   └── accepted-pool.js
│   ├── v3/               # Public scripts
│   │   ├── app.js
│   │   └── accepted-pool.js
│   └── version-switcher.js  # ملف التحكم في التبديل
└── data/
    ├── v1/               # Hybrid data
    │   ├── campaign-data.js
    │   ├── campaigns-data.js
    │   └── data.js
    ├── v2/               # Invite data
    │   ├── campaign-data.js
    │   ├── campaigns-data.js
    │   └── data.js
    └── v3/               # Public data
        ├── campaign-data.js
        ├── campaigns-data.js
        └── data.js
```

---

## 🎯 كيفية الاستخدام

### 1️⃣ التبديل بين النسخ

**أثناء العرض للعميل:**
- ستجد 3 أزرار في أعلى يمين كل صفحة:
  - **Hybrid** - النسخة الهجينة
  - **Invite** - نسخة الدعوات
  - **Public** - النسخة العامة
- اضغط على أي زرار للتبديل بين النسخ
- الصفحة ستُعاد تحميلها تلقائياً بالتصميم الجديد
- النسخة المختارة محفوظة في المتصفح (localStorage)

### 2️⃣ تعديل النسخة Hybrid (v1)

```bash
# عدّل ملفات CSS في مجلد v1
/css/v1/styles.css
/css/v1/applications.css
# ... إلخ

# عدّل ملفات JS في مجلد v1
/js/v1/app.js
/js/v1/accepted-pool.js
```

### 3️⃣ تعديل النسخة Invite (v2)

```bash
# عدّل ملفات CSS في مجلد v2
/css/v2/styles.css
/css/v2/applications.css
# ... إلخ

# عدّل ملفات JS في مجلد v2
/js/v2/app.js
/js/v2/accepted-pool.js
```

### 4️⃣ تعديل النسخة Public (v3)

```bash
# عدّل ملفات CSS في مجلد v3
/css/v3/styles.css
/css/v3/applications.css
# ... إلخ

# عدّل ملفات JS في مجلد v3
/js/v3/app.js
/js/v3/accepted-pool.js
```

---

## 💡 أمثلة للتعديل

### مثال 1: تغيير الألوان في Invite (v2)

```css
/* افتح ملف /css/v2/styles.css */
/* غيّر متغيرات الألوان: */

:root {
    --primary: #FF6B6B;      /* لون أحمر بدلاً من #6366F1 */
    --primary-dark: #E05555;
    --primary-bg: #FFE5E5;
}
```

### مثال 2: تغيير الألوان في Public (v3) - نسخة خضراء

```css
/* في /css/v3/styles.css */

:root {
    --primary: #10B981;      /* لون أخضر */
    --primary-dark: #059669;
    --primary-bg: #D1FAE5;
}
```

### مثال 3: تغيير الخطوط في Invite

```css
/* في /css/v2/styles.css */

body {
    font-family: 'Cairo', sans-serif;  /* بدلاً من Plus Jakarta Sans */
}
```

### مثال 4: تعديل البيانات في Public

```bash
# افتح /data/v3/campaign-data.js
# عدّل أي بيانات تريدها لعرض مختلف
```

---

## 🚀 سيناريو العرض للعميل

```
1. افتح المشروع على localhost
   → python3 -m http.server 8000

2. افتح http://localhost:8000

3. ابدأ العرض بالنسخة Hybrid (الافتراضية)
   → اشرح المميزات والتصميم

4. اضغط على زرار Invite
   → الصفحة تُعاد تحميلها بتصميم نسخة الدعوات

5. اضغط على زرار Public
   → شاهد النسخة العامة

6. قارن بين النسخ الثلاث
   → العميل يشوف الاختلافات ويختار الأفضل

7. انتقل بين الصفحات
   → النسخة المختارة تظل ثابتة في كل الصفحات
```

---

## 🎨 التخصيص المتقدم

### معلومات عن النسخ

**النسخ الثلاث حالياً متطابقة** - تم نسخ نفس التصميم للثلاثة:
- `v1` (Hybrid) - النسخة الأساسية
- `v2` (Invite) - نسخة الدعوات
- `v3` (Public) - النسخة العامة

**اقتراحات للتخصيص:**

#### نسخة Hybrid (v1):
- احتفظ بالتصميم الحالي
- ألوان أزرق/بنفسجي
- للحملات المختلطة (عامة + دعوات)

#### نسخة Invite (v2):
- غيّر الألوان لـ أحمر/وردي
- أضف عناصر تركز على الدعوات الخاصة
- للحملات المغلقة

#### نسخة Public (v3):
- غيّر الألوان لـ أخضر
- أضف عناصر للحملات العامة المفتوحة
- للحملات التي تقبل أي مؤثر

---

## 🔧 استكشاف الأخطاء

### المشكلة: النسخة لا تتغير

**الحل:**
1. افتح Developer Console (F12)
2. امسح localStorage:
   ```javascript
   localStorage.clear()
   ```
3. أعد تحميل الصفحة

### المشكلة: الأزرار لا تظهر

**الحل:**
1. تأكد من تحميل ملف version-switcher.js:
   ```html
   <script src="js/version-switcher.js"></script>
   ```
2. تأكد من وجود الأزرار في HTML:
   ```html
   <div class="version-switcher">
       <button class="version-btn active" data-switch-version="hybrid">Hybrid</button>
       <button class="version-btn" data-switch-version="invite">Invite</button>
       <button class="version-btn" data-switch-version="public">Public</button>
   </div>
   ```

### المشكلة: CSS لا يتحمل

**الحل:**
تأكد من المسارات الصحيحة في HTML:
```html
<!-- للصفحات في المجلد الرئيسي -->
<link rel="stylesheet" href="css/v1/styles.css" data-version-file="styles.css">

<!-- للصفحات في مجلد pages -->
<link rel="stylesheet" href="../css/v1/styles.css" data-version-file="styles.css">
```

---

## 📝 ملاحظات مهمة

1. **حالياً النسخ الثلاث متطابقة** - تم نسخ نفس التصميم للثلاثة
2. **عدّل كل نسخة حسب رغبتك** لإنشاء تصاميم مختلفة
3. **النظام يحفظ اختيارك** - حتى لو أغلقت المتصفح
4. **جميع الصفحات متزامنة** - اختيار النسخة يطبق على كل الصفحات
5. **الأسماء معبرة** - Hybrid/Invite/Public توضح طبيعة كل نسخة

---

## ✅ ملخص الميزات

- ✅ تبديل سريع بين 3 نسخ مختلفة
- ✅ أسماء معبرة (Hybrid/Invite/Public)
- ✅ حفظ تلقائي للاختيار
- ✅ واجهة بسيطة وأنيقة
- ✅ يعمل على جميع الصفحات (12 صفحة)
- ✅ سهل التعديل والتخصيص
- ✅ مناسب لعرض المشروع للعملاء

---

**استمتع بعرض المشروع! 🚀**
